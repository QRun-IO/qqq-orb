# orb.yml is "packed" from source, and not published directly from the repository.
orb.yml
.DS_Store
target

# Test data directory (created dynamically during tests)
tests/test_data

# IDE 
.idea

.claude
