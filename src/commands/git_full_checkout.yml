description: >
  Full checkout of the repository with complete git history.
  Uses CircleCI's checkout (handles SSH keys and known_hosts)
  then fetches full history for version calculation.

steps:
  - checkout
  - run:
      name: Fetch full git history
      command: |
        git fetch --unshallow || true
        git fetch --tags || true
