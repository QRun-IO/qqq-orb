description: >
  Publishes the package to NPM
  - Sets up NPM authentication
  - Publishes the package to NPM

steps:
  - run:
      name: "Setup NPM authentication"
      command: <<include(scripts/node_npm_auth.sh)>>
  - run:
      name: "Publish to NPM"
      command: <<include(scripts/node_npm_publish.sh)>>
